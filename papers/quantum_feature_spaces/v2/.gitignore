# Generated dataset artifacts from the Generator stage
# (raw pools, teacher weights, metadata — reproducible from configs + seeds).
datasets/
datasets_v2/

shots_v2/
# Kernel embeddings / Gram caches from the embedding stage
# (reproducible from the embedding config + dataset).
embeddings/


img/